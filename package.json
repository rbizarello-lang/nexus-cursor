{
  "name": "nexus-fiscal",
  "version": "1.1.6",
  "private": true,
  "description": "NEXUS — Painel de Operações Fiscais (build local do JSX)",
  "scripts": {
    "build": "node scripts/build.mjs",
    "version:bump": "node scripts/bump-version.mjs",
    "version:patch": "node scripts/bump-version.mjs patch",
    "version:minor": "node scripts/bump-version.mjs minor",
    "version:major": "node scripts/bump-version.mjs major",
    "push": "node scripts/bump-version.mjs patch && npm run build && node scripts/sync-gas.mjs && npx --yes @google/clasp push",
    "push:minor": "node scripts/bump-version.mjs minor && npm run build && node scripts/sync-gas.mjs && npx --yes @google/clasp push",
    "push:major": "node scripts/bump-version.mjs major && npm run build && node scripts/sync-gas.mjs && npx --yes @google/clasp push"
  },
  "devDependencies": {
    "@babel/core": "^7.26.0",
    "@babel/preset-env": "^7.26.0",
    "@babel/preset-react": "^7.26.0"
  }
}
