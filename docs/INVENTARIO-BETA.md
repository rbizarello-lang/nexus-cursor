# Inventário da edição Beta — o que mudou desde o clássico estável

**Para:** Roger (decisão item a item: adotar ou rejeitar)  
**Data deste inventário:** 18/09/2026  
**Commit de base (versão anterior a todo este trabalho):** `1dc76ea` (`1dc76ea5263b2cac89932ee2f62b6e08a6efa1d7`)  
**Não foi feito commit.** Nenhum arquivo deste inventário altera o app.

**Como usar:** cada linha é uma mudança isolável (ou quase). Marque **adotar** ou **não**. As dependências entre linhas estão no final. **Itens numerados: 1 a 202** (202 linhas de decisão).

**Notas de QA (telas de conferência):** pasta `C:\Users\User\AppData\Local\Temp\nexus-audit\qa-final\` — clássico em `classic-head\` / `classic\`; Beta em `beta\` (ex.: `beta\01-hoje.png`, `beta\03-mesa.png`, `beta\05-gaveta.png`). Plano que guiou o trabalho: `C:\Users\User\AppData\Local\Temp\nexus-audit\PLANO-BETA.md`.

**HTML gerado (`Nexus.html`, `Nexus.demo.html`) ignorado neste inventário** — é saída de `npm run build`.

## Arquivos tocados e volume

| Arquivo | Linhas no diff / tamanho | Papel |
|---|---|---|
| `src/lib/prescription.js` | +588 / −126 (714 no `--stat`) | Motor de prazos e radar |
| `src/lib/dates.js` | +5 / −3 | Aniversário de datas (29/02) |
| `src/lib/prazos-mesa.js` | **novo**, 196 linhas | Frases e recorte da Mesa (Beta) |
| `src/app.jsx` | +871 / −390 (1261 no `--stat`) | Telas clássico + Beta |
| `src/Nexus.shell.html` | +156 / −95 (251 no `--stat`) | CSS e portal de dicas |
| `scripts/build.mjs` | +6 / −4 | Compila o JS da Mesa no HTML |
| `package.json` | +1 / −1 | Versão 2.0.1 → **2.0.7** |
| `MOTOR_PRESCRICAO.md` | +1 | Histórico **2026.09a** |
| `docs/MELHORIAS.md` | +7 / −9 | Demo Experimental → Nova versão (beta) |
| `test/prescription.test.mjs` | +1 / −1 | Status `interrompido` |
| `test/presc-v2.test.mjs` | **novo**, 437 linhas, **27** testes | Correções 2.A + política v2 |
| `test/prazos-mesa.test.mjs` | **novo**, 113 linhas, **10** testes | Mesa |

**Total do diff rastreado:** 9 arquivos, **+1636 / −629**.  
**Testes:** **196** antes → **233** depois (+27 v2 +10 Mesa).

**Chaves novas em `localStorage` (`nexus_settings`):** `prazosDeskMode` (`mesa` \| `lista`). A chave antiga `demoTheme` deixa de ser lida. `processViewModel` é forçado a `D`. A chave solta `nexus_demo_proc_view` deixa de ser gravada. `uiEdition: 'demo'` continua sendo o interruptor interno da Beta.

**Campos opcionais novos no cadastro da CDA (não quebram o clássico):** `debt.prescSnooze` (`{ until, reason, at, group, note? }`) e `debt.statusSource` (`'auto'` quando a Beta grava status sozinha).

---

## (A) Motor de prescrição — correções de conta (clássico e Beta)

Estas linhas mudam o **número e o texto** que as duas edições mostram nas três colunas da CDA e, no clássico, na lista “Prazos extintivos”. Rejeitar alguma delas **mantém o erro de conta correspondente**.

| # | Item | Técnico | Em linguagem simples | Onde vale | Reversível isoladamente? | Decisão |
|---|---|---|---|---|---|---|
| 1 | Pausas sobrepostas fundidas | `src/lib/prescription.js` — `pauseIntervals`: passa a unir intervalos que se encostam ou se cobrem antes de descontar. Usado por `addUnpausedCalendarYears`, `computeIntercorrente` (dias sem pausa) e `computeIntercorrenteBounds`. Teste `ARITH-PAUSE-OVERLAP` em `test/presc-v2.test.mjs`. | Se a CDA teve embargos de 2018 a 2020 **e** depósito de 2019 a 2021, o app somava os dois períodos inteiros e empurrava o termo **um ano para a frente**. Agora conta o calendário real (2018–2021). Exemplo de regressão: termo esperado **01/01/2025**, não 2026. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 2 | Primeira ciência inicia o ciclo | `computeIntercorrente`: se já existe `marco` e o ciclo não foi encerrado, ciência nova vira ocorrência e **não** substitui `diesAQuo`. `occurrenceEffect` devolve “nova certidão — não reinicia”. | Uma segunda certidão de não localização **não recomeça** o 1 ano + 5 anos. A data de início continua a da **primeira** ciência. No teste: ciências em 01/01/2018 e 01/06/2019 → termo **01/01/2024**, não 2025. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 3 | Evento com data depois de hoje — pausas | `applyPausesFromEvents`: só lê eventos com `date <= asOf`. | Uma adesão a parcelamento datada no futuro **não pausa** o prazo de hoje. Evita “sumir” da fila por um fato que ainda não aconteceu. | Clássico + Beta | sim com ressalva: combina com #4–#6 | [ ] adotar  [ ] não |
| 4 | Evento futuro no crédito (ordinária) | `computeOriginario`: se `efetivacao > asOfIso`, grava `gaps` “Evento com data futura (…) — ignorado no cômputo” e segue. | Na coluna de prescrição ordinária, um fato datado depois de hoje aparece em **Conferir nos autos**, sem alterar o termo. | Clássico + Beta | sim com ressalva: parte de #3–#6 | [ ] adotar  [ ] não |
| 5 | Evento futuro na intercorrente | `computeIntercorrente`: constrição com efetivação futura continua como “pedido sem resultado”; **qualquer outro** tipo futuro vira gap “evento com data futura” e não entra na conta. Rescisão futura **não** vira novo início. | Exemplo do teste: rescisão em 01/06/2027, hoje 17/09/2026 → o ciclo **não** recomeça em 2027; termo segue 01/01/2028 a partir da ciência de 2022. | Clássico + Beta | sim com ressalva: parte de #3–#6 | [ ] adotar  [ ] não |
| 6 | Parcelamento futuro não conta como vigente | `parcelamentoVigentePorEvento` / `isCdaParcelada`: ignora adesão/rescisão com `date > asOf`. | Status ou evento “parcelada a partir do ano que vem” não tira a CDA da fila **hoje**. | Clássico + Beta | sim com ressalva: parte de #3–#6 | [ ] adotar  [ ] não |
| 7 | Conferir: data futura e ciência antes do ajuizamento | Nova `buildCadastroChecks`; gaps de futuro e de ciência anterior viram itens de **Conferir nos autos**. | O cálculo continua, mas a coluna avisa: “ciência anterior ao ajuizamento — conferir data” ou o texto do evento futuro. | Clássico + Beta | sim com ressalva: depende de #4–#5 e #8 | [ ] adotar  [ ] não |
| 8 | Ciência anterior ao protocolo | `computeIntercorrente`: se a ciência é anterior a `exec.protocolDate` / `debt.protocolDate`, grava o gap (cálculo **mantido**). | Não apaga a data. Só pede conferência. Útil quando alguém lançou certidão de 2018 numa execução ajuizada em 2020. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 9 | Termo já passou + pedido sem resultado | `buildSummary`, `sealIntercorrente`, `pendingPetitionDate`: situação deixa de ser “Prazo em curso” com dias negativos. Frase: “Termo calculado em dd/mm já passou; há pedido de dd/mm sem resultado — conferir antes de declarar”. Classificador: `kind: 'vencido'` com rótulo **pedido pendente** (não `residual_alta`). | Evita a contradição “em curso” com “−990 dias”. O vermelho continua, com o selo de que há petição pendente. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 10 | Artigo pela / pelo | `INTERRUPT_PREP`, `interruptType`, `interruptPrep`, `interruptFactLabel`. `buildSummary` do ciclo encerrado: “Ciclo encerrado **pelo** arresto / **pelo** bloqueio Sisbajud / **pela** penhora / **pela** citação”. | Corrige “pela arresto” e “pela bloqueio Sisbajud”. Na tela das três colunas, a frase fica gramaticalmente certa. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 11 | 29 de fevereiro | `src/lib/dates.js` — `addCalendarYears`: se o dia não existe no ano-alvo, usa o **último dia de fevereiro** (28/02), não 01/03. Afeta todo aniversário em anos (1+5, piso, etc.). | Ciência em 29/02/2024 → termo em **28/02/2030**, não 01/03/2030. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 12 | Ordinária ajuizada: status `interrompido` | `computeOriginario` (dois retornos): `status: 'seguro'` → `'interrompido'`. `phase` já era `interrompido`. Teste antigo em `test/prescription.test.mjs` e `STATUS-SEGURO-ORDINARIA`. | A coluna deixa de chamar de “seguro” uma CDA só porque a execução foi ajuizada. O selo passa a **Interrompida — vigiar** (`CDA_SEGMENT_STATUS.interrompido`). | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 13 | Art. 40 na coluna: data e “vale como ciência” | `CASE_FACT.susp_art40` = “Suspensão do art. 40”. `occurrenceEffect` para `susp_art40`. `columnDates`: reconhece “vale como ciência” e ciclo `art40` para **não** escrever “sem ciência lançada” quando o início existe. `buildIncidentChecks`: se o gap é “sem evento de marco”, pede confirmação da certidão. | Se o processo só tem “suspensão do art. 40” em 01/06/2015, a coluna passa a mostrar **Início: 01/06/2015**, ocorrência “Suspensão do art. 40 … vale como ciência”, e Conferir: “certidão de não localização/sem bens não lançada — confirmar data”. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 14 | Conferir de incidente só na intercorrente | `withCaseView`: `buildIncidentChecks` só se `r.segment === 'intercorrente'`. Cadastro (conflito de data, constituição faltando) foi para `buildCadastroChecks` (todas as colunas). | O aviso “IDPJ sem constrição” **não se repete** na coluna da ordinária. Continua na intercorrente. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 15 | Índice de eventos por execução | `buildPrescCollectIndex` agora **exportada**; ganha mapa `byExec`. `collectEventsForCda`, `matchingExecsForDebt`, `eventsTouchingDebt`, `hasDatedPrescEvent`, `hasParcelamentoEvent`, `cadastroInconsistencia`, `isCdaParcelada`, `computeOrdinaria`, `computeCdaLegalTimeline`, `attachPrescriptionSnapshots` e o radar usam o índice. | Mesmo resultado, menos varredura. Foi feito porque o radar travava em carteira grande (achado PERF-RADAR-N2). Você não vê diferença numa operação demo de 5 nomes. | Clássico + Beta | sim com ressalva: recuar só o índice sem desfazer as novas chamadas quebra o radar | [ ] adotar  [ ] não |
| 16 | CDA só no IDPJ marcada `incidentOnly` | `collectEventsForCda` devolve `incidentOnly`; `computePrescription` / `computeOrdinaria` / `withCaseView` / linhas do painel copiam o flag (antes só um gap textual). | Não muda a fila **clássica** sozinho. Na Beta (política v2, #27) essa CDA pede “vincule à execução fiscal”. | Clássico + Beta (flag); efeito de fila só Beta | sim; o efeito de tela é #27 | [ ] adotar  [ ] não |
| 17 | `isCdaParcelada` com 5º argumento | Assinatura `(debt, executions, events, asOf, fifth)`: `fifth` pode ser o índice ou `{ collectIndex, ignoreStatus }`. Sem o 5º arg, o clássico segue: status parcelada **ou** adesão vigente tira da fila. | No clássico, CDA com status “parcelada” **sem** evento de adesão **continua saindo** da lista de prazos (como antes). A novidade de “ficar na fila” é só v2 (#25). | Clássico + Beta (API); comportamento clássico igual | sim | [ ] adotar  [ ] não |
| 18 | Texto de `susp_art40` no catálogo | `CASE_FACT.susp_art40`: “Registro de suspensão do art. 40” → “Suspensão do art. 40”. | Só o rótulo curto na tabela de ocorrências. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 19 | Histórico 2026.09a (motor) | `MOTOR_PRESCRICAO.md` (seção Histórico). O `scripts/build.mjs` injeta esse markdown em `<!--INJECT_PRESC_RULES-->` (`#presc-rules-doc`). A constante `RULE_VERSION` **permanece** `'2026.09'` — o rodapé das colunas e o ⚙ ainda dizem “Regras v2026.09”. | Depois de gerar o HTML (`npm run build`), a janela ⓘ **Regras de prazos** nas duas edições ganha o parágrafo **2026.09a** (18/09/2026) descrevendo as contas novas. O número da versão **no título** da janela continua “v2026.09”. Sem o build, só o arquivo `.md` no computador muda. | Clássico + Beta (texto da página de regras) | sim | [ ] adotar  [ ] não |
| 20 | Mortos **não** removidos | Continuam no código: `PRESC_FLAGS.PARC_SEM_FIM`, `avaliar_174`, `pessimisticIntercorrenteFloor`, `CDA_SEGMENT_STATUS`. | Nada some. O plano permitia apagar se ninguém usasse; a regra do projeto é não remover sem pedido. | só código | — (não há o que adotar) | [ ] adotar  [ ] não |

---

## (B) Motor — política v2 do radar (só Beta)

O clássico chama `buildPrazosRadar(...)` **sem** `policy` → continua **v1**. A Beta chama `buildPrazosRadar(..., { policy: 'v2' })` em `prazosRadar` e na importação de análise.

| # | Item | Técnico | Em linguagem simples | Onde vale | Reversível isoladamente? | Decisão |
|---|---|---|---|---|---|---|
| 21 | Interruptor `policy: 'v1' \| 'v2'` | `resolvePolicy`, `resolveRadarTail`. Parâmetro extra em `classifyPainelPrescAlert`, `isPainelPrescCandidate`, `groupOfKind`, `buildPainelPrescAlerts`, `buildPrazosRadar`. Default **v1**. | Sem este interruptor, a Mesa e as regras novas da fila não existem. O clássico não passa o parâmetro, então **não muda de fila**. | só código (o efeito visível é Beta) | não — depende de quase todo o Bloco B | [ ] adotar  [ ] não |
| 22 | Beta usa v2; clássico v1 | `src/app.jsx` — `prazosRadar` `useMemo`: `isDemo ? { policy: 'v2' } : undefined`. Importação NotebookLM: `buildPrazosRadar(next, undefined, undefined, isDemo ? { policy: 'v2' } : undefined)`. | Abrir `Nexus.html` = fila antiga. Abrir `Nexus.demo.html` ou ⚙ → Nova versão (beta) = fila nova. | só Beta (o clássico é a ausência) | não — depende de #21 | [ ] adotar  [ ] não |
| 23 | Kind novo `aguardando_reconhecimento` | `PAINEL_PRESC_KINDS`, `prazosKeyMeta`, `groupOfKind` → grupo 4. | Na Beta, CDA marcada “aguardando reconhecimento” **não some**. Vai para acompanhamento / Silenciados, selo “aguardando decisão”. No clássico, continua **fora** da aba Prazos. | só Beta | não — depende de #21 e #24 | [ ] adotar  [ ] não |
| 24 | `aguardando_reconhecimento` permanece candidata | `isPainelPrescCandidate` + `HANDLED_TERMINAL` (`declarada`, `reconhecida`, `extinta`). `wrapAguardando` em `classifyPainelPrescAlert`. | Só declaração em juízo / reconhecimento / CDA extinta saem de vez. “Aguardando” vira acompanhamento, não alarme. | só Beta | sim com ressalva: sem #23 o kind não tem grupo | [ ] adotar  [ ] não |
| 25 | Ficha parcelada sem adesão = inconsistência | v2: se **não** há evento `susp_parcelamento` vigente mas o status é `parcelada` / `negociada_sispar` / execução `suspensa_parcelamento` → `kind: 'inconsistencia'`, `action: { type:'criar_evento', eventType:'susp_parcelamento' }`, frase “Ficha diz parcelada, sem adesão lançada”. Cálculo **segue sem pausa**. | No demo, CDAs da Laranjas / Holding com parcelamento **com evento** vão para Silenciados (#33). Uma ficha só “parcelada” na caneta **continua na Mesa** pedindo a data da adesão. No clássico, status sozinho ainda **esconde**. | só Beta | não — depende de #21; a ação de um clique é #114 | [ ] adotar  [ ] não |
| 26 | Ordinária consumada em CDA ajuizada → grupo 1 | v2, ramo ajuizado sem ciclo intercorrente: se `computeOrdinaria` está `consumado` / vencida → `kind: 'vencido'`, `clock: 'ordinaria'`, rótulo “ordinária”, grupo 1. | CDA ajuizada cuja prescrição **dos 5 anos da inscrição** já venceu deixa de cair no grupo 5 (“ainda impossível”) e some do alarme. Na Beta vira urgente com selo ordinária. | só Beta | não — depende de #21 | [ ] adotar  [ ] não |
| 27 | CDA só no IDPJ pede vínculo à EF | v2 + `incidentOnly`: `inconsistencia`, `action: { type:'vincular_ef' }`, rótulo “vincule à execução fiscal”. | Número da CDA aponta para IDPJ/cautelar, não para execução fiscal. A Beta não trata isso como “sem processo”: pede para ligar à EF. | só Beta | não — depende de #16 e #21 | [ ] adotar  [ ] não |
| 28 | Piso futuro permanece grupo 5 | `groupOfKind(..., { policy:'v2' })`: `acompanhar_piso` → 5 **antes** do teste de cadastro. v1: incidente sem constrição **roubava** para grupo 3. | CDA da Sucessão com IDPJ `5008890-77.2025.4.04.7003` e piso ainda longe **não** sobe para “completar cadastro”. O aviso do IDPJ vira nota (#29), não alarme. | só Beta | não — depende de #21 | [ ] adotar  [ ] não |
| 29 | Aviso de incidente uma vez por processo | `buildPrazosRadar` v2: `processNotes[]`; `seenIncident` por `normProc`. Tira o check repetido “não tem constrição lançada” de **cada** CDA. | Duas CDAs da mesma EF não ganham o mesmo recado duas vezes. **Atenção:** a UI da Mesa **não lista** `processNotes` (não há tela para esse array). O recado some da linha da CDA; a gaveta G5 ainda mostra a CDA. | só Beta | sim com ressalva: a nota não aparece em lugar nenhum da tela | [ ] adotar  [ ] não |
| 30 | Análise importada não rebaixa grupo 1 | v2: se o kind calculado é grupo 1 e a decisão importada pediria grupo maior, **mantém 1**. v1: a decisão importada governava. | Uma análise “ciclo encerrado” **não apaga** o vermelho de termo já vencido na Beta. Continua anotada (`decisionNote`). | só Beta | não — depende de #21 | [ ] adotar  [ ] não |
| 31 | `why` (uma frase) e `action` (um clique) | `radarWhyAction`; campos na linha do radar só se `policy==='v2'`. Frases fechadas por kind (vencido, iminente, parcelada sem adesão, vincular EF, etc.). Tipos de ação: `criar_evento`, `conferir_autos`, `corrigir_ficha`, `vincular_ef`, `lancar_ciencia`, `nenhuma`. | Cada linha da Mesa mostra **por que está ali**, em português curto, sem “Tema/Súmula/piso”. Exemplo QA: CDA `90.6.23.000884-40` (Agro Horizonte, ITR, R$ 48.000) — “O termo calculado cai nos próximos 180 dias.” | só Beta | não — a Mesa (#84–#123) lê esses campos | [ ] adotar  [ ] não |
| 32 | `reviewAt` fora dos grupos 1 e 2 | `reviewAtFor`: pausa → fim conhecido ou +90d; `vigiar_interrompido` → interrupção+1 ano (mínimo hoje+30d); piso → data do piso; aguardando → +90d; inconsistência → +30d. | A Mesa usa isso para puxar ao bloco “PRECISA DE VOCÊ” quando a data de rever já passou (#86). | só Beta | não — depende de #21 e da Mesa | [ ] adotar  [ ] não |
| 33 | Lista `silenced` | Radar v2 devolve `silenced: [{ debtId, reason, until, label, group }]`. Inclui parcelamento **vigente por evento** (`reason: 'parcelamento_vigente'`, until +90d) e itens adiados (#34). v1: `silenced` é `undefined`. | No QA da Beta: gaveta **SILENCIADOS (15)** — ex. CDAs `90.6.22.000777-01` etc. “Parcelamento vigente · até 17/12/2026”. Não somem: ficam na gaveta. | só Beta | não — a gaveta (#109) depende disto | [ ] adotar  [ ] não |
| 34 | Adiar (`debt.prescSnooze`) no motor | `PRESC_SNOOZE_REASONS`, `snoozeLimitDays` (G1 14d, G2 30d, G3 7d, G5 90d, demais 30d), `snoozeEffectiveUntil`, `snoozePierced`. Fura o silêncio se: `until` passou, grupo piorou, novo evento após `at`, motivo `outro` sem texto, ou razão desconhecida. | Você esconde a linha até uma data. Se no meio disso o caso **piorar** (novo fato ou grupo mais grave), ela volta sozinha. Campo novo e **opcional** na CDA; o clássico ignora. | só Beta (gravação na UI); o campo existe no JSON das duas | não — UI do Adiar é #112 | [ ] adotar  [ ] não |
| 35 | Constantes de adiar exportadas | `export const PRESC_SNOOZE_REASONS` e `export function snoozeLimitDays`. | Lista fechada na tela: Aguardando certidão, Peça protocolada, Garantia em análise, Não priorizar agora, Outro. | só código + Beta | não — depende de #34 | [ ] adotar  [ ] não |

---

## (C) Correções também aplicadas ao clássico (interface)

Tudo abaixo aparece em **Nexus.html** (clássico) **e** na Beta, salvo onde o texto diz o contrário. O dono precisa saber: o clássico **não** ficou bit a bit igual ao `1dc76ea`.

| # | Item | Técnico | Em linguagem simples | Onde vale | Reversível isoladamente? | Decisão |
|---|---|---|---|---|---|---|
| 36 | Campos mínimos obrigatórios | `formRequiredError` em `src/app.jsx`; `EntityFormRouter.save` bloqueia e mostra `formError`. Intimação: nº **ou** descrição; CDA: número; tarefa: título; bem: descrição **ou** matrícula; operação: nome. | Sem o número da CDA, o Salvar não grava e aparece a faixa vermelha “Informe o número da CDA.” O mesmo para intimação sem processo e sem descrição. Vale nos dois modos. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 37 | Faixa de erro no formulário | CSS `.form-inline-error` em `src/Nexus.shell.html`; bloco no `Actions` do `EntityFormRouter`. | Recado vermelho **dentro** da janela, acima de Cancelar/Salvar — não é mais só o campo vazio. | Clássico + Beta | sim com ressalva: visual de #36 | [ ] adotar  [ ] não |
| 38 | Esc fecha a janela | `Modal`: `keydown` Escape chama `onClose`. | A tecla Esc fecha criação/edição de CDA, intimação, tarefa, etc. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 39 | Clique fora fecha; se digitou, pergunta | `requestCloseModal` + `modalDirtyRef` + `onDirtyChange` no `EntityFormRouter`. Overlay do `Modal` já chamava `onClose`; agora `onClose` é `requestCloseModal`. Confirma: “Há texto digitado. Fechar sem salvar?” | Clique no fundo escuro fecha. Se você já escreveu, o navegador pergunta antes de jogar fora. | Clássico + Beta | sim com ressalva: usa o mesmo Esc (#38) | [ ] adotar  [ ] não |
| 40 | Atributos `data-focus` nos campos | Inputs de data do evento, data de prescrição da CDA e nº do processo ganham `data-focus`. Foco automático só quando a Mesa manda `_focusDate` / `_focusField` (Beta). | No clássico os atributos existem e não mudam o uso. Na Beta, ao clicar “Lançar fato”, o cursor cai na data. | Clássico + Beta (atributo); foco automático só Beta | sim | [ ] adotar  [ ] não |
| 41 | Aviso curto ao salvar | `showToast` + estado `flashToast` (some em 2,8 s). `handleSave`: “Intimação salva”, “Tarefa salva”, “CDA salva”, “Operação salva”, senão “Salvo”. | Depois de Salvar, um recado aparece no rodapé da tela. | Clássico + Beta | sim com ressalva: CSS #43 | [ ] adotar  [ ] não |
| 42 | Aviso ao registrar atuação na intimação | `handleRespondIntim`: ciência → “Ciência registrada — ver Resolvidas”; peticionamento → “Peticionamento registrado — ver Resolvidas”; resto → “Atuação registrada — ver Resolvidas”. | Confirma que a intimação foi para Resolvidas, sem abrir a aba. | Clássico + Beta | sim com ressalva: CSS #43 | [ ] adotar  [ ] não |
| 43 | CSS do aviso flutuante | `.flash-toast` no shell. | Caixa central na parte de baixo da tela. | Clássico + Beta | sim com ressalva: #41–#42 | [ ] adotar  [ ] não |
| 44 | Painel ⚙ — botão ✕ | `renderSettingsPanel`: cabeçalho `.settings-panel-hd` + `.settings-panel-close` “✕”. | Dá para fechar o ⚙ pelo X, não só clicando de novo na engrenagem. | Clássico + Beta | sim com ressalva: CSS #48 | [ ] adotar  [ ] não |
| 45 | Painel ⚙ — clicar fora fecha | `.settings-backdrop` (camada invisível) + `onClick` → `setShowSettings(false)`. | Clique em qualquer lugar fora do retângulo de ajustes fecha o painel. | Clássico + Beta | sim com ressalva: CSS #48 | [ ] adotar  [ ] não |
| 46 | Painel ⚙ — Esc fecha | Atalho global Escape: além da busca, agora `setShowSettings(false)` (e fecha o menu “Mais” da Beta). Antes o Esc também fechava a gaveta Trabalho da Demo, que não existe mais. | Esc fecha o ⚙ nas duas edições. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 47 | Painel ⚙ fecha ao trocar de vista | `useEffect` em `viewMode`: `setShowSettings(false)`. | Se o ⚙ está aberto e você vai de Painel para Intimações, ele fecha. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 48 | CSS do ⚙ (fecho) | `.settings-backdrop`, `.settings-panel-hd`, `.settings-panel-close`. | Só o desenho do X e da área clicável. | Clássico + Beta | sim com ressalva: #44–#45 | [ ] adotar  [ ] não |
| 49 | Rótulo “Nova versão (beta)” no ⚙ | Botão da edição: era “Demo Experimental” (ou “Demo” no HTML standalone). Texto de ajuda: “A nova versão (beta) usa a mesma navegação do clássico, com Hoje e Agenda unificada…” | No clássico, o ⚙ **ainda mostra** o interruptor para a Beta, agora com esse nome. QA: `classic\settings` / `settingsFeat.editionBtns`. | Clássico + Beta (o texto do ⚙) | sim | [ ] adotar  [ ] não |
| 50 | Botão “Resetar / carregar dados demo” saiu da barra esquerda | Removido do bloco Google Sheets da **sidebar** (`!isGAS`). Continua no ⚙ → Dados / Sync. | A coluna da esquerda, nas duas edições, não tem mais o botão de tubo de ensaio. Só ⚙. O QA clássico marca `cloudHasDemo: false`. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 51 | Setas ⬇ ⬆ do rodapé da sidebar ganham título | `title="Exportar JSON"` e `title="Importar JSON"`. | Ao parar o mouse nas setas, o sistema mostra o nome. Não muda o clique. QA: `footerTitles`. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 52 | Selo NÃO AJ / AJ com `title` por extenso | Cards de inscrição: `title="Não ajuizada"` / `title="Ajuizada"` (o `tip-content` longo **já existia**). | Pausa curta no selo vermelho **NÃO AJ** da CDA `90.8.21.000200-02` (Agro Horizonte, FGTS) mostra “Não ajuizada”. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 53 | Selo de espécie do processo (A / IA / etc.) por extenso | Visão D: `especie-badge` ganha `has-tip` + `tip-content` com `species.label` (além do `title` que já existia). | O código curto (A, IA…) ganha a frase completa ao passar o mouse. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 54 | Entradas vazias somem do Briefing | `briefingEntryHasText`, `normalizeBriefingEntry`, `BRIEFING_TITLE_TO_TYPE`; `getBriefingEntries` filtra. **Não** está atrás de `isDemo`. | Cartão “Observação” sem texto **não aparece** mais nas duas edições. Títulos antigos (Hipótese, Situação, Pendências…) são mapeados para os tipos novos. Os dados demo (Fachada Norte, etc.) continuam visíveis porque têm corpo. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 55 | Semana da agenda do Painel: Mês→Semana cai na semana **de hoje** | `renderAgendaWeek` → `goWeek`: **não** está atrás de `isDemo`. Antes, ao sair do mês, ia para a semana do dia 1 daquele mês. | No **Painel clássico**, o controle Semana/Mês/Hoje do quadro semanal, ao voltar para Semana, mostra a semana corrente (ex. QA: 14–20 set 2026), não a primeira semana do mês que você estava olhando. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 56 | Versão exibida 2.0.7 | `package.json` `"version": "2.0.7"` injetada em `window.__NEXUS_VERSION__`. | O ⚙ mostra **NEXUS 2.0.7** (QA 18/09/2026). | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 57 | `data-cda-id` na linha da CDA (visão processos) | Atributo no `cda-row` para o scroll até as colunas. | No clássico não muda o visual. Na Beta, “Abrir” / “Conferir” rola até essa linha (#118). | Clássico + Beta (atributo) | sim | [ ] adotar  [ ] não |
| 58 | Limpeza de campos internos ao salvar evento/CDA | `handleSave` apaga `_focusDate`, `_focusField` para não gravar lixo no JSON. | Evita persistir flags de interface no arquivo / Planilha. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 59 | Fonte padrão no ⚙ da Beta deixou de dizer “Padrão (Demo)” | O botão vazio de fonte sempre rotula **Public Sans** (as duas edições). | Só muda o ⚙ quando você está na Beta (no clássico já dizia Public Sans). | Clássico + Beta (mesmo rótulo) | sim | [ ] adotar  [ ] não |
| 60 | Tema clássico visível também na Beta | O grupo de tema saiu do `!isDemo &&`. Na Beta o rótulo vira **Aparência** (#140). No clássico o rótulo continua **Tema**, com Mar Profundo · Claro · Ferro e Maré. | No clássico a lista de temas **já existia**. A mudança clássica é só compartilhar o mesmo bloco de código. | Clássico + Beta | sim com ressalva: o rótulo Aparência é #140 | [ ] adotar  [ ] não |

---

## (D) Edição Beta — estrutura (layout, Hoje, Agenda, top-nav, o que saiu da Demo)

A flag interna continua `uiEdition: 'demo'` (`isDemo`). O nome na tela passou a **Nova versão (beta)**. HTML: `Nexus.demo.html`, `<title>NEXUS Beta`.

| # | Item | Técnico | Em linguagem simples | Onde vale | Reversível isoladamente? | Decisão |
|---|---|---|---|---|---|---|
| 61 | Layout igual ao clássico | `app-layout` da Beta **não** esconde mais `.sidebar` / `.top-nav`. Classes `demo-rail-expanded`, `demo-carteira-open`, `demoThemeClass` saíram do `className`. Beta recebe `appSettings.theme` (Mar/Claro/Ferro). | A Beta ganhou a coluna da esquerda e a faixa de abas de cima, como o clássico. Não é mais o “trilho” estreito da Demo. Screenshot: `qa-final\beta\01-hoje.png`. | só Beta | não — base de #62–#80 | [ ] adotar  [ ] não |
| 62 | Paleta da Demo Clara/Ardósia/Grafite desligada | `.app-layout.edition-demo` no shell: removidos os tokens papel-ardósia, Fraunces/IBM, trilho 76px e o fundo em degradê. Comentário: paleta vem dos temas clássicos. | A Beta **clara** que você via na Demo standalone some. Vale Mar Profundo / Claro / Ferro e Maré, iguais ao clássico. | só Beta | sim com ressalva: CSS morto dos temas Demo ainda está no arquivo (#81) | [ ] adotar  [ ] não |
| 63 | `demoTheme` ignorado | `DEMO_THEMES_OK`, `demoThemeDefault`, seletor “Tema da Demo” e gravação em `nexus_settings.demoTheme` removidos da leitura. | Quem tinha salvo “Ardósia” na Demo volta para o tema clássico gravado (em geral Mar Profundo). | só Beta | sim | [ ] adotar  [ ] não |
| 64 | Trilho esquerdo da Demo removido | JSX do `<nav className="demo-rail">` (Hoje / Intimações / Carteira-árvore / Agenda / Biblioteca / Trabalho) apagado. CSS: `.edition-demo .demo-rail { display: none !important; }`. | Não há mais a barra preta “Central de Comando” à esquerda. | só Beta | não — junto com #61 | [ ] adotar  [ ] não |
| 65 | Faixa superior “Demo” removida | JSX `demo-topbar` (título grande, pílula Demo, Busca, + Operação) apagado. CSS `display: none`. | Some o título enorme “Hoje / Carteira / Biblioteca” e a pílula **Demo**. | só Beta | não — junto com #61 | [ ] adotar  [ ] não |
| 66 | Zonas da operação (Briefing/Acervo/Risco/Ferramentas) removidas | `DEMO_ZONES`, `demoZone`, `setDemoZoneAndTab`, `demo-zones` / `demo-zone-sub` / `demo-zone-panel`. A operação Beta usa as **mesmas abas** do clássico (Briefing, Pessoas, Inscrições…). | Dentro da Agro Horizonte, você volta a ver Briefing · Pessoas · Inscrições · Processos e Prescrição · Bens · Tarefas · Importar · Arquivos — não mais quatro “zonas”. | só Beta | sim | [ ] adotar  [ ] não |
| 67 | Gaveta Trabalho da Demo removida | Estados `demoTrabalhoOpen` e o drawer (Mesa / Acompanhar / Painel KPIs / Prazos). | O chevron “Trabalho” da Demo some. Mesa e Acompanhar estão na top-nav. | só Beta | sim | [ ] adotar  [ ] não |
| 68 | Árvore Carteira no trilho removida | `carteiraTreeOpen`, `carteiraContext`, `openCarteiraHome`/`openCarteiraOp` simplificados (não abrem a sidebar à força). | Não há lista de operações **dentro** do trilho. Operações voltam à sidebar clássica. | só Beta | sim | [ ] adotar  [ ] não |
| 69 | Painel de ranking da Carteira desligado | `{false && isDemo && renderCarteiraRankingPanel()}`. A função `renderCarteiraRankingPanel` **permanece** no arquivo, sem tela. | Some o bloco “Painel da Carteira / Maior valor de crédito / 1. Operação Sucessão…” que a Demo punha em Operações. | só Beta | sim | [ ] adotar  [ ] não |
| 70 | Visões A/B/C de Processos removidas | Seletor `demo-proc-view-switch`; `processViewModel` sempre `'D'`; não grava mais `nexus_demo_proc_view`. | Na Beta, Processos é só a visão master–detail (igual ao clássico). Não há mais A árvore / B card / C tabela. | só Beta | sim | [ ] adotar  [ ] não |
| 71 | Top-nav Beta: 9 itens + operação | `betaNavItems`: Hoje · Painel · Prazos · Operações · Intimações e Tarefas · Mesa · Acompanhar · Agenda · Modelos; depois o nome da operação truncado (`top-nav-op`, 22 caracteres). O item Intimações e Tarefas usa `extra: openIntimsCount + openTasksCount` e marca ativo em `intimacoes` **ou** `tarefas_global`. | A faixa de cima da Beta **não** diz “Prazos extintivos” nem “Audiências”: diz **Prazos** e **Agenda**. **Intimações** e **Tarefas** viram **um** botão, com a soma dos dois números. Por dentro, as abas Intimações / Tarefas (`demo-inbox-switch`) continuam. A **Mesa** desta faixa é a antiga (pins de intimação), não a Mesa de prazos. | só Beta | não — base da navegação Beta | [ ] adotar  [ ] não |
| 72 | Itens que não cabem vão para “Mais ▾” | `useLayoutEffect` + `ResizeObserver` em `betaNavRef`; classe `is-overflow`; menu `.top-nav-mais-menu`. Estado `betaMaisOpen` / `betaNavOverflow`. | Se a janela ou o zoom não couber, os últimos itens saem da faixa e entram no botão **Mais ▾**. Não quebra linha. | só Beta | não — depende de #71 | [ ] adotar  [ ] não |
| 73 | CSS da top-nav Beta | `.edition-demo .top-nav` nowrap, `.top-nav-cluster`, `.top-nav-btn` 11px, `.top-nav-op` ellipsis, `.top-nav-mais`. | Desenho da faixa para não quebrar. | só Beta | não — depende de #71–#72 | [ ] adotar  [ ] não |
| 74 | Vista **Hoje** como primeira e inicial | `switchEdition('demo')` se estava em Painel vai para `hoje`. `switchEdition('classic')` se estava em Hoje volta para `painel`. `loadDemoData` na Beta abre `hoje`. Bootstrap `?edition=demo` / `Nexus.demo.html` já caía em demo; a vista inicial da Beta é Hoje (não Painel). | Ao ligar a Beta pelo ⚙, a primeira tela é **Hoje** (“O que exige ação hoje?”). Ao voltar para Clássico estando em Hoje, cai no **Painel** (o clássico não tem a aba Hoje). | só Beta (ida); o retorno afeta o clássico | sim com ressalva: a vista Hoje é #75; o retorno é #153 | [ ] adotar  [ ] não |
| 75 | Textos da vista Hoje | Kicker: “NEXUS · Nova versão (beta)” (era “NEXUS Demo · Central de Comando”). Subtítulo sem a frase dos matter hubs. CTA “Ver Carteira” → “Ver operações”. Removido o botão de resetar dados demo da própria Hoje. Vazio: “Use Operações ou Intimações…” (não “Carteira”). | A tela Hoje fala Beta, não Demo. O atalho para dados fictícios ficou só no ⚙. | só Beta | sim | [ ] adotar  [ ] não |
| 76 | Hoje: Nova intimação já traz a operação aberta | CTA e `EntityFormRouter` (Beta, intimação nova sem id): `operationId` da operação ativa. | Se você está na Agro Horizonte e clica Nova intimação, o campo operação já vem preenchido. | só Beta | sim | [ ] adotar  [ ] não |
| 77 | Atalhos da fila Hoje não usam zonas | `buildHojeFila` para tarefa: `startTabSwitch(() => setActiveTab('tarefas'))` em vez de `setDemoZoneAndTab('ferramentas', …)`. | Clicar uma tarefa na fila Hoje abre a aba Tarefas da operação, no layout clássico. | só Beta | sim | [ ] adotar  [ ] não |
| 78 | Vista Audiências vira **Agenda** unificada | `viewMode === 'audiencias'`: se `isDemo`, prefixa `renderAgendaWeek()` (grade semana/mês) **e** mantém a lista de audiências. | Na Beta, o item Agenda mostra o quadro da semana (como o Painel) **mais** os cards “Esta semana / Este mês”. No clássico a aba continua só a lista, nome **Audiências**. | só Beta | sim com ressalva: grade é a mesma função do Painel (#55) | [ ] adotar  [ ] não |
| 79 | Controles da grade na Beta: ‹ › e Hoje à direita | Em `renderAgendaWeek`, se `isDemo`: botões `‹` / `›` (aria Anterior/Próximo) e **Hoje** depois. Clássico mantém “← Semana / Hoje / Semana →”. | Só muda os rótulos e a ordem na Beta. | só Beta | sim | [ ] adotar  [ ] não |
| 80 | Título da janela e tela de carregamento | `scripts/build.mjs` `assembleHtml(demo)`: `<title>NEXUS Beta</title>` (era “NEXUS Demo — Central de Comando”). Boot: “Carregando NEXUS 2.0.7 Beta…”. | A aba do navegador diz **NEXUS Beta**. | só Beta | sim com ressalva: build #146 | [ ] adotar  [ ] não |
| 81 | CSS residual da Demo antiga (não aparece) | Permanecem no shell, **sem classe aplicada**: `.demo-theme-mar/ardosia/grafite`, `.demo-zones`, `.demo-trabalho-drawer`, `.demo-proc-view-switch`, regras do trilho. Apenas escondidos (`display:none` no trilho/topbar). | Você **não vê** Ardósia/Grafite. É CSS morto no arquivo, não uma tela extra. | só código | sim (limpeza futura; não muda tela) | [ ] adotar  [ ] não |
| 82 | Fontes Inter/Outfit/Source na Demo | Removidos os blocos `.edition-demo.font-inter` etc. que forçavam a fonte no layout Demo. Vale a mesma herança do clássico (`html.font-*`). | Trocar a fonte no ⚙ da Beta usa o mesmo mecanismo do clássico. | só Beta | sim | [ ] adotar  [ ] não |
| 83 | Fundo do panorama no Briefing | `.edition-demo .briefing-pano-strip-sticky` usa `var(--bg-deep)` em vez de `demo-glass-soft`. | A faixa de chips do panorama acompanha o tema clássico. | só Beta | sim | [ ] adotar  [ ] não |

---

## (E) Beta — Prazos / Mesa de trabalho

Arquivo novo `src/lib/prazos-mesa.js` (funções puras) + bloco em `src/app.jsx` (`renderPrazosMesa`, `renderMesaRow`, …). Só corre se `isDemo`. Preferência `nexus_settings.prazosDeskMode` (padrão **mesa**).

| # | Item | Técnico | Em linguagem simples | Onde vale | Reversível isoladamente? | Decisão |
|---|---|---|---|---|---|---|
| 84 | Seletor **Mesa \| Lista** | `prazosDeskMode`, `setPrazosDeskMode`, `renderPrazosMesaToggle`. Lista = a tela clássica de grupos; Mesa = a nova. | Na Beta, Prazos abre em **Mesa**. Dá para voltar à lista de grupos (Urgentes / A conferir / …) sem sair da Beta. O clássico não tem o seletor. | só Beta | sim (volta à lista); a Mesa em si é #85+ | [ ] adotar  [ ] não |
| 85 | Constante teto 12 | `export const MESA_CAP = 12`. | No máximo **12** linhas no bloco de cima. A 13ª vira o botão vermelho de orçamento (#92). | só Beta | não — junto com #91 | [ ] adotar  [ ] não |
| 86 | O que entra em “PRECISA DE VOCÊ” | `mesaNeedsYou`: grupo 1 sempre; grupo 2 só se `prescDays <= 0`; grupo 3 só se ação em `MESA_ONE_CLICK` (`criar_evento`, `corrigir_ficha`, `vincular_ef`); qualquer grupo se `reviewAt` já passou. | Alarme só para o que pede decisão agora. G3 “só conferir autos” **não** sobe. QA: duas linhas — `90.6.23.000884-40` e `90.6.20.000881-40` (Agro Horizonte, sem processo, CALCULADO, termo nos 180 dias, R$ 48.000 e R$ 95.000). | só Beta | não — depende de #31 e #21 | [ ] adotar  [ ] não |
| 87 | Recorte needs / resto / G5 | `splitMesaRows`: ordena needs por grupo e dias; G5 sai do resto (`hiddenG5`) para a gaveta. | “Ainda impossível” **não infla** o bloco do meio. Vai para Silenciados. | só Beta | não — gaveta #109 | [ ] adotar  [ ] não |
| 88 | Selo de certeza | `mesaCertainty`: calculado / estimado / cadastro conforme kind/grupo. CSS `.mesa-cert.calculado\|estimado\|cadastro`. | Cada linha tem um selozinho verde/laranja/amarelo. Nas duas CDAs da Agro acima: **CALCULADO**. | só Beta | sim | [ ] adotar  [ ] não |
| 89 | Frase da linha (`why`) | `renderMesaRow` usa `betaSafeUiText(r.why \|\| r.prescLabel)`. | Uma frase, sem Tema/Súmula. | só Beta | não — depende de #31 e #99 | [ ] adotar  [ ] não |
| 90 | Layout da linha | CSS `.mesa-row`, `.mesa-cda` (mono), `.mesa-proc`, `.mesa-why`, `.mesa-val`, `.mesa-row-actions`. CDA · processo · selo · frase · valor · botões. | A linha cabe o essencial sem a tabela de cinco colunas do clássico. | só Beta | não — bloco Mesa | [ ] adotar  [ ] não |
| 91 | Bloco PRECISA DE VOCÊ | `section.mesa-needs` + `h2`. Vazio: “Nada exige decisão agora. O restante está abaixo, sem alarme.” Borda esquerda vermelha. | É o alarme da Beta. Screenshot `beta\03-mesa.png`. | só Beta | não — depende de #86 | [ ] adotar  [ ] não |
| 92 | “+K acima do orçamento” | Botão `.mesa-overcap`; estado `mesaOverCapOpen`; lista o `overCap`. | Se houver 15 urgentes, mostra 12 e um botão **+3 acima do orçamento** (vermelho, clicável). | só Beta | não — depende de #85 | [ ] adotar  [ ] não |
| 93 | G1 vencido com o peso de VENCIDA | Classe `g1-vencido` na linha (`.mesa-row.g1-vencido`, faixa vermelha). `isG1Vencido`. | Termo já passado ganha o mesmo vermelho das intimações vencidas. | só Beta | sim | [ ] adotar  [ ] não |
| 94 | Selo “expirou o silêncio” | Se a CDA ainda tem `prescSnooze` **e** voltou para a fila (furo do motor), `.mesa-expired`. | Depois que o adiamento cai, a linha volta com o aviso de que o silêncio acabou. | só Beta | não — depende de #34 | [ ] adotar  [ ] não |
| 95 | Botões Evento / Abrir / Conferir / Adiar… | Sempre na linha. Evento → `openPrescEventForRow`. Abrir/Conferir → `openCdaInscricoes(..., { scrollCols: true })`. | Quatro ações fixas, mais o botão extra de um clique quando couber (#113). | só Beta | sim com ressalva: Evento pré-preenchido é #115 | [ ] adotar  [ ] não |
| 96 | Bloco **O RESTO** | Linha clicável com contagens `G1 n · G2 n · G3 n · G4 n` e G5 se houver; chevron; `mesaRestoOpen`. Grupos 1–4 ao expandir, com `PRAZOS_GROUP_LABELS`. | QA: “O RESTO G1 0 · G2 0 · G3 1 · G4 0 · G5 9”. Um clique expande. Sem alarme vermelho. `beta\04-mesa-resto.png`. | só Beta | sim | [ ] adotar  [ ] não |
| 97 | CSS Mesa (toolbar, blocos, drawer, snooze) | Bloco “Beta: Mesa de prazos” no shell: `.mesa-view` (flex, height 0 + grow para o rodapé não cobrir linhas), `.mesa-scroll-body`, `.mesa-footer-dock` (static), toolbar, overcap, silenced-bar, drawer max 280px, popover `.mesa-snooze`. | Desenho da Mesa e da gaveta. O QA `retest.json` checou que o botão de orçamento **não** fica escondido atrás da barra Silenciados. | só Beta | não — visual da Mesa | [ ] adotar  [ ] não |
| 98 | Filtros da Mesa | Select de operação + busca “CDA, processo ou devedor” (`prazosFilters`, já existia na lista). | Mesmo recorte da lista clássica, na Mesa. | só Beta | sim | [ ] adotar  [ ] não |
| 99 | Limpeza de jargão na UI Beta | `betaSafeUiText`: tira Tema 566/568, Súmula, troca política/piso/teto/dies/marco/CENÁRIO. Usado na Mesa, na **Lista** Beta (`prazos-sum` / `prazos-check`) e nas três colunas se `isDemo`. | Na Beta você não lê “piso” nem “Tema” nas frases do dia a dia. A memória técnica / página de regras **não** passa por este filtro. | só Beta | sim (pode deixar o jargão na Mesa se rejeitar só isto) | [ ] adotar  [ ] não |
| 100 | Colunas da CDA na Beta sem travessão e sem jargão | `CdaPrescColumns(..., isDemo)`: `safe()`; datas “—” → “sem datas” / “sem data”. | As três colunas nunca mostram um “—” solto. | só Beta | sim com ressalva: #99 | [ ] adotar  [ ] não |
| 101 | Exportes da Mesa | `MESA_ONE_CLICK`, `mesaCertainty`, `isG1Vencido`, `mesaNeedsYou`, `splitMesaRows`, `formatPrescHorizon`, `betaCdaPrescText`, `mesaDrawerItems`, `countSnoozeDueThisWeek`, `snoozeMaxUntil`, `betaSafeUiText`, `betaEventFamilyLabel`, `betaEventFamilyDesc`. | Biblioteca da Mesa; o build concatena o arquivo no HTML (#146). | só código | não — a UI importa estes nomes | [ ] adotar  [ ] não |
| 102 | Frase da linha da CDA (lista Inscrições / card / tabela D) | `betaPrescMeta` + `betaCdaPrescText`. Nunca “—”. Tratada: “Tratada em dd/mm”. Conflito: “Ficha 26/01/2027 · app 14/08/2031 — conferir”. Estimativa não pode dizer “Prescrita” (`stripPrescritaIfEstimate`). Sem linha no radar: “sem ciência lançada” ou o `silenced.label`. | Na Inscrições da Agro, em vez de “25/02/2027 (160d)” ou “—”, a Beta escreve a frase do radar. CDA tratada mostra a data do tratamento, não a contagem. | só Beta | não — depende de #31 e #33 | [ ] adotar  [ ] não |
| 103 | Dias > 730 viram anos | `formatPrescHorizon`: −800 → “há 2 anos”; 1100 → “em 3 anos”; nunca “(-61034)”. Usado na linha da CDA e no resumo de hubs se Beta. | Prazos longos ficam legíveis. | só Beta | sim | [ ] adotar  [ ] não |
| 104 | Hubs: não escrever “Prescrita” no resumo | Resumo de seção master–detail: Beta usa `formatPrescHorizon` / “em acompanhamento”, não a palavra Prescrita para minRiskDays ≤ 0. | Evita chamar de prescrita uma estimativa no card do processo. | só Beta | sim com ressalva: #102 | [ ] adotar  [ ] não |
| 105 | Tag “(informada)” escondida na linha Beta | `Prescrição{!isDemo && prescTag(d) ? …}`. | A Beta não acrescenta “(informada)” do lado do rótulo; o conflito vai na frase (#102). | só Beta | sim | [ ] adotar  [ ] não |
| 106 | Card de processo: VENCIDA no mesmo peso da intimação | `anyG1Vencido` → `process-risk` + `intim-card overdue` + `<span className="intim-deadline overdue">VENCIDA</span>`. CSS `.process-risk.g1-vencido strong`. | Se alguma CDA do processo está no grupo 1 vencido, o card grita **VENCIDA** como o card de intimação. | só Beta | sim | [ ] adotar  [ ] não |
| 107 | KPI da operação: some “risco (1+2)” | `opStats.prescG1` / `prescG1Vencido` / `prescG3`. Subtítulo: VENCIDA, ou “N a completar”, ou “em dia”. Número = só grupo 1. Classe `intim-card overdue` se houver G1 vencido. | No cabeçalho da Agro Horizonte, o clássico mostra “PRESC. CDA **2** risco (1+2)”. A Beta conta só o urgente calculado e, se já venceu, o selo VENCIDA. | só Beta | sim | [ ] adotar  [ ] não |
| 108 | KPI do Painel honesto | Quarto widget: rótulo **Prazos**; valor `{n1} urgentes · {n3} para completar cadastro · {fmtCur(v1)}`; sub “só o que pede decisão agora”. Tooltip explica G1 e G3. Clássico mantém “Risco prescricional” n1+n2. | QA `beta\02-painel-kpi.png`: “**2 urgentes · 1 para completar cadastro · R$ 143.000,00**”. O valor é só das urgentes, não soma o grupo 2. | só Beta | sim | [ ] adotar  [ ] não |
| 109 | Gaveta Silenciados | `mesaDrawerItems` junta `silenced` + `silenceReason` + G5. Barra `.mesa-silenced-bar`; botão “SILENCIADOS (N) ▸”; `mesaSilencedOpen`. | QA: **SILENCIADOS (15)**. CDA `90.6.19.000047-88` (Sucessão, PIS/COFINS, R$ 890.000) aparece como ainda impossível até 02/05/2028, não como alarme. `beta\05-gaveta.png`. | só Beta | não — depende de #33 e #87 | [ ] adotar  [ ] não |
| 110 | “K adiamentos vencem esta semana” | `countSnoozeDueThisWeek` (até +7 dias, só razões de adiar). `.mesa-silenced-week`. | Se um Adiar acaba nesta semana, a barra avisa em amarelo. | só Beta | não — depende de #34 | [ ] adotar  [ ] não |
| 111 | Linha da gaveta + Reabrir agora | Motivo, `until`, botão só se `canReopen` (adiamento do usuário, não parcelamento vigente). `clearPrescSnooze` zera `prescSnooze`. Toast “Reaberta na mesa”. | QA: após Adiar `90.6.23.000884-40` com “Peça protocolada” até 02/10/2026, a gaveta tem **Reabrir agora**. Parcelamento vigente **não** tem esse botão. | só Beta | não — depende de #34 | [ ] adotar  [ ] não |
| 112 | Popover Adiar… | `renderMesaSnoozePopover`: motivo (lista #35), data `min=hoje` `max=snoozeMaxUntil`, texto obrigatório se Outro. `applyPrescSnooze`. Toast “Adiada até …”. Sem motivo/data não grava. | QA `beta\07-adiar-gaveta.png`: a CDA ITR some da Mesa e vai para a gaveta. Limite: grupo 1 no máximo 14 dias. | só Beta | não — depende de #34–#35 | [ ] adotar  [ ] não |
| 113 | Ação extra de um clique | Se `action.type` não é `nenhuma`/`conferir_autos` e não é o atalho de parcelamento: botão primário “Lançar fato” / “Vincular EF” / “Corrigir ficha” / “Lançar ciência”. `applyMesaAction`. | Um clique abre o formulário certo. Vincular EF foca o nº do processo; corrigir ficha foca a data de prescrição. | só Beta | não — depende de #31 | [ ] adotar  [ ] não |
| 114 | Parcelada sem adesão: data + Enter | Se ação é `criar_evento` + `susp_parcelamento`: input de data na linha, “Enter lança a adesão”. `createInlineParcelamento` grava `susp_parcelamento`. Toast “Adesão lançada”. | Completar o cadastro **sem** abrir o modal grande. | só Beta | não — depende de #25 | [ ] adotar  [ ] não |
| 115 | + Evento / Evento já traz CDA, EF e tipo | `openPrescEventForRow`: `cdaId`, `executionId`, `operationId`, `type`/`_familyId` se houver, `_focusDate: true`. Na **Lista** Beta o botão + Evento também usa isto. | QA `beta\09-evento-prefill.png`: título “Novo(a) Evento Prescricional”, CDA `90.6.23.000884-40 — R$ 48.000`. (Se a linha não tem EF, o select permanece “Selecione…”.) | só Beta | sim | [ ] adotar  [ ] não |
| 116 | Destino IDPJ/cautelar: rótulo da família | `betaEventFamilyLabel` / `betaEventFamilyDesc`: família `resultado_util` + destino incidente → “**Constrição no incidente (pausa as EFs)**” e texto sem “Penhora / resultado útil”. Família `marco` → “Ciência do art. 40”. | Ao lançar fato num IDPJ, o menu não diz “Penhora / resultado útil” como se fosse a execução. | só Beta | sim com ressalva: #99 | [ ] adotar  [ ] não |
| 117 | Ajuda do evento e da CDA na Beta sem “Tema 566/568” | `helpOf` = `betaSafeUiText` nas descrições de família/tipo; Sisbajud: “se houve constrição” em vez de “resultado útil”. Formulário da CDA (Beta): some “(política interna)”; “⏱ Marcos do crédito…” vira “Datas do crédito (decadência / prescrição ordinária)”; dica da rescisão sem Tema 566; dica da data de prescrição: “Data digitada. Se o app já calculou o termo, esta data aparece como conflito — não cala o cálculo.” (some “marco ou ciclo pós-parcelamento”). | O modal de evento **é compartilhado**. Só a Beta troca os textos. O clássico continua com Tema 566 e Súmula 622 nas dicas. | só Beta | sim | [ ] adotar  [ ] não |
| 118 | Abrir CDA rola até as três colunas | `openCdaInscricoes(d, { scrollCols })`; `cdaScrollColsRef`; `scrollIntoView` em `.cda-presc-cols` (`block: 'start'`). Beta sempre pede scroll. | QA `beta\10-conferir.png` / `11-abrir-colunas.png`: cai na aba Inscrições, colunas visíveis, botões Copiar/Evento/Editar no topo. | só Beta | sim com ressalva: #57 e #119 | [ ] adotar  [ ] não |
| 119 | Barra grudada Copiar / Evento / Editar | `CdaLegalDetail` e o detalhe inline do processo, se `isDemo`: `.cda-actions-sticky`. | Os três botões não somem quando você rola as colunas. | só Beta | sim | [ ] adotar  [ ] não |
| 120 | Mini-formulário “Arquivada art. 40” | Ao marcar processo **arquivada** na Beta, em vez do `confirm` das CDAs: modal `art40Form` (título “Arquivada art. 40 — lançar a data”). Campos: “O que você tem em mãos?” (`ciencia` / `arquivo`); se ciência, “Tipo do registro” (`susp_art40` / `marco_sem_bens`); data; botões **Agora não** / **Lançar**. Sem data: toast “Informe a data”. `commitArt40Form` cria um evento **por CDA ligada**. Toast “N evento(s) lançado(s)”. Extinta na Beta **ainda** usa o confirm antigo. | Em vez de perguntar só sim/não, pede a data. Opções na tela: “Data da ciência (não localização / sem bens)”, “Data do arquivamento”, “Suspensão do art. 40 (vale como ciência)”, “Ciência de ausência de bens”. QA `retest.json`. | só Beta | sim | [ ] adotar  [ ] não |
| 121 | Status garantida **não** sai de Sisbajud/CNIB/IDPJ | `statusMap` em `handleSave` de evento: na Beta, só `int_penhora` e `int_arresto` (e os suspensivos de sempre) mudam status. `int_sisbajud`, `int_cnib`, `susp_idpj_mcf_constricao` **não** gravam `garantida`. Quando grava, manda `statusSource: 'auto'`. | Bloqueio Sisbajud na Beta **não** pinta a CDA de garantida sozinho. Penhora/arresto ainda pintam, com marca `statusSource:'auto'`. O clássico **continua** promovendo Sisbajud/CNIB/IDPJ a garantida. | só Beta | sim | [ ] adotar  [ ] não |
| 122 | Lista clássica (modo Lista da Beta) usa frases limpas | `prazos-sum` / `prazos-check` passam por `betaSafeUiText` se `isDemo`. Toggle Mesa\|Lista também aparece na Lista. | Se você recusar a Mesa mas adotar a Beta, a lista de grupos ainda perde o jargão. | só Beta | sim | [ ] adotar  [ ] não |
| 123 | `processNotes` calculado e **não desenhado** | Radar v2 preenche `prazosRadar.processNotes`; **nenhum** JSX lê o campo. | Item de motor (#29) sem tela. Rejeitar #29 ou deixar: hoje o usuário não vê essa lista. | só Beta / só código | sim | [ ] adotar  [ ] não |

---

## (F) Beta — demais telas

| # | Item | Técnico | Em linguagem simples | Onde vale | Reversível isoladamente? | Decisão |
|---|---|---|---|---|---|---|
| 124 | Intimações: vencidas primeiro no modo prazo | Se `isDemo` e ordenação `deadline` / `days_left` / `overdue_first`: compara vencida vs no prazo, depois o dia, depois Urgente só como desempate. | No modo padrão “Prazo final (mais próximo)”, a intimação **já vencida** (ex. Fachada Norte `5009876-11.2024.4.04.7001` se estiver atrasada) sobe **antes** das que ainda têm prazo, mesmo que alguma no prazo esteja marcada Urgente. | só Beta | sim | [ ] adotar  [ ] não |
| 125 | Blocos “Vencidas” / “Urgentes no prazo” | `betaDeadlineBlocks`; cabeçalhos `.intim-block-h.vencidas\|urgentes`. | A lista se parte em dois títulos coloridos (vermelho / amarelo). | só Beta | não — depende de #124 | [ ] adotar  [ ] não |
| 126 | CSS dos blocos de intimação | `.intim-block-h` no shell. | Só o desenho dos títulos. | só Beta | não — depende de #125 | [ ] adotar  [ ] não |
| 127 | Título “Nova intimação” | `modalTitle`: create + intimation + isDemo. | A janela não diz “Novo(a) Intimação”. | só Beta | sim | [ ] adotar  [ ] não |
| 128 | Salvar grudado no rodapé (intimação) | `Modal` `stickyFooter={isDemo && entityType==='intimation'}`; CSS `.modal-sticky-footer`. | Em intimação longa, Cancelar/Salvar permanecem visíveis embaixo. Outros formulários não. | só Beta | sim | [ ] adotar  [ ] não |
| 129 | Tarefas da operação: um número + subtítulo | Aba Tarefas: `{N} aberta(s)` e linha “G globais · M só na operação”. Some o itálico longo do clássico. Cabeçalho da operação: subtítulo de tarefas vira a mesma conta (`taskGlobalN` / `taskOpOnlyN`). | Em vez de “4 aberta(s) · 0 concluída(s)”, a Beta diz quantas são globais. | só Beta | sim | [ ] adotar  [ ] não |
| 130 | Tarefas gerais (menu): a mesma conta | Vista `tarefas_global`: conta globais vs `taskVisibility==='operation'`. | O número único no topo da lista geral segue a mesma lógica. | só Beta | sim com ressalva: #129 | [ ] adotar  [ ] não |
| 131 | Selo ATRASADA Nd | `.task-overdue-seal` nas duas listas de tarefas se `days < 0`. | Visual igual ao vencido das intimações. Ex.: tarefa “Analisar viabilidade de redirecionamento” se o prazo já passou. | só Beta | sim com ressalva: CSS #132 | [ ] adotar  [ ] não |
| 132 | CSS do selo ATRASADA | `.task-overdue-seal`. | Faixa vermelha pequena. | só Beta | não — depende de #131 | [ ] adotar  [ ] não |
| 133 | Toast “Relatório gerado” | `gerarRelatorio` / passagem de serviço: `if (isDemo) showToast('Relatório gerado')`. | Depois do HTML de passagem de serviço, um recado curto (além do download). | só Beta | sim | [ ] adotar  [ ] não |
| 134 | Resumo da operação começa recolhido na Beta | `opHeaderCollapsed`: se não há `nexus_op_header_collapsed`, URL demo / `__NEXUS_DEMO__` → `true`. `switchEdition('demo')` força recolhido. | KPIs DÍVIDA TOTAL / GARANTIDO / PRESC. CDA começam **fechados**; o botão ▴/▾ abre. Atende “chips/KPIs recolhidos”. Os chips do panorama do Briefing **já** nasciam fechados (`panoFocusId` null) — isso não mudou. | só Beta | sim | [ ] adotar  [ ] não |
| 135 | Busca “Filtrar processo / CDA” nas Inscrições | Estado `procCdaQuery`; input na toolbar da aba Inscrições; filtra número da CDA e processo (também só dígitos). | Digite `884-40` e sobra a CDA ITR da Agro. | só Beta | sim | [ ] adotar  [ ] não |
| 136 | A mesma busca em Processos e Prescrição | Substitui o seletor A/B/C; filtra hubs, EFs, extintas, outros, sem vínculo. | Um campo só para achar processo **ou** CDA na visão D. | só Beta | sim com ressalva: #135 (mesmo estado) | [ ] adotar  [ ] não |
| 137 | Chip de classificação com zero some | `opClassChipKeys(..., { hideEmpty: isDemo })` no filtro da sidebar e na lista Operações. | “Parcelamento Integral (0)” **não aparece** na Beta. No clássico o chip zerado continua. | só Beta | sim | [ ] adotar  [ ] não |
| 138 | KPIs da operação numa linha compacta | Classe `stats-bar beta-compact` + CSS (nowrap, ellipsis, valor 13px). Também `.edition-demo.theme-claro .stats-bar`. | Tema Claro + zoom 90: os oito cartões do cabeçalho não quebram a palavra no meio. | só Beta | sim | [ ] adotar  [ ] não |
| 139 | Dicas (tooltip) abaixo do alvo | CSS `.edition-demo .has-tip > .tip-content` (abaixo + seta invertida). Portal em `Nexus.shell.html`: `preferBelow` se existe `.edition-demo`; sobe só se não couber na janela. | As dicas não cobrem a Agenda nem o nome da operação na faixa de cima. | só Beta | sim | [ ] adotar  [ ] não |
| 140 | Rótulo **Aparência** no ⚙ | `settings-label`: `isDemo ? 'Aparência' : 'Tema'`. | Na Beta, o grupo Mar/Claro/Ferro chama-se Aparência. | só Beta | sim | [ ] adotar  [ ] não |
| 141 | Abrir CDA a partir do radar (clássico vs Beta) | `openCdaInscricoes` não chama mais `setDemoZoneAndTab`; sempre `setActiveTab('dividas')`. | Caminho único. No clássico, “Abrir” na aba Prazos já ia para Inscrições; só deixa de existir o desvio das zonas. | Clássico + Beta (simplificação) | sim | [ ] adotar  [ ] não |
| 142 | Importação de análise respeita v2 | `commitNexusPrescricao` + `buildPrazosRadar(..., policy v2)` só na Beta, para o resumo do que piorou. | Colar análise na Beta compara com a fila nova. | só Beta | não — depende de #21 | [ ] adotar  [ ] não |

---

## (G) Documentação, build e testes

| # | Item | Técnico | Em linguagem simples | Onde vale | Reversível isoladamente? | Decisão |
|---|---|---|---|---|---|---|
| 143 | Histórico 2026.09a | `MOTOR_PRESCRICAO.md` linha nova em Histórico das regras (18 set 2026). | Texto para humanos na página de regras (as contas do Bloco A). | Clássico + Beta (página ⓘ) | sim | [ ] adotar  [ ] não |
| 144 | `RULE_VERSION` **não** foi para 2026.09a | `prescription.js` `RULE_VERSION = '2026.09'`. ⚙ → “Versão 2026.09”. Rodapé das colunas “Regras v2026.09”. | O número na tela **não** avisa que as contas mudaram; só o parágrafo do histórico (#19/#143). | Clássico + Beta | — (ausência) | [ ] adotar  [ ] não |
| 145 | Banco de melhorias reescrito | `docs/MELHORIAS.md` seção Demo → “Nova versão (beta)”. **Não** atualiza o “Última atualização: 03/08/2026” do topo. | O caderno de anotações descreve Hoje + Agenda + 3 temas. Não sobe no Apps Script. | só código / docs | sim | [ ] adotar  [ ] não |
| 146 | Build injeta `prazos-mesa.js` | `scripts/build.mjs` concatena o arquivo entre `prescription.js` e `presc-import.js`. Comentário do arquivo: “edição Beta”. | Sem isto a Mesa nem as frases existem no HTML gerado. | só código | não — a Beta precisa | [ ] adotar  [ ] não |
| 147 | Mensagens do build | Boot e `console.log`: “NEXUS Beta” no segundo HTML. | Só o terminal e a tela “Carregando…”. | só Beta / toolchain | sim | [ ] adotar  [ ] não |
| 148 | 27 testes da política e das contas | `test/presc-v2.test.mjs` (LEAP, overlap, marco, futuro, petição, ciência antes, gramática, seguro, R10, conferir duplicado, v1 regressão, parcelada, aguardando, ordinária, IDPJ, G5, decisão importada, snooze, silenced). | Rede de segurança: se alguém recusar um item do motor, o teste correspondente falha. | só código | sim (apagar o arquivo) | [ ] adotar  [ ] não |
| 149 | 10 testes da Mesa | `test/prazos-mesa.test.mjs` (needsYou, cap 12, G5, certeza, horizonte, frases da CDA, jargão, gaveta, semana, teto de adiar). | Idem para a Mesa. | só código | sim | [ ] adotar  [ ] não |
| 150 | Um assert antigo ajustado | `test/prescription.test.mjs`: espera `status === 'interrompido'` em vez de `seguro`. | Acompanha #12. | só código | não — depende de #12 | [ ] adotar  [ ] não |
| 151 | Suíte 196 → 233 | `package.json` `test` continua `node --test test/*.test.mjs`. | 37 testes a mais. Rodar `npm test` deve ficar verde se adotar o motor + Mesa. | só código | — | [ ] adotar  [ ] não |
| 152 | Catálogo das frases `why` | `radarWhyAction` em `prescription.js`: vencido+ordinária → “Os 5 anos da inscrição venceram antes do ajuizamento.”; vencido+pedido pendente → “O termo calculado já passou, mas há pedido sem resultado nos autos.”; vencido → “O prazo de 1 ano + 5 anos já venceu no cálculo.”; iminente → “O termo calculado cai nos próximos 180 dias.”; vencido_estimado → “Pelo cadastro, a consumação já é a hipótese mais provável — conferir nos autos.”; residual_alta → “Falta ciência lançada e o prazo operacional já apertou.”; parcelada sem adesão → “A ficha diz parcelada, mas não há adesão lançada — o prazo segue correndo.”; vincular EF → “O número apontado é de incidente, não de execução fiscal.”; outra inconsistência → “Há dado da ficha sem o fato correspondente nos eventos.”; aguardando → “A prescrição já foi apontada e aguarda decisão judicial.”; piso → “Ainda não pode ter prescrito: o ato mais recente mais 1 ano e 5 anos não chegou.”; vigiar_interrompido → “O ciclo encerrou por resultado útil; vigiar nova inércia.”; pausa → “O prazo está pausado por fato lançado; conferir se a pausa ainda vale.”; residual_media → “Sem ciência lançada; a data de acompanhamento já passou, sem agravante.”; correndo → “O prazo de 1 ano + 5 anos está em curso.”; senão → “Esta inscrição está na fila de prazos.” | São as frases que a Mesa e a linha da CDA mostram. Recusar o catálogo e manter a Mesa deixa as linhas mudas ou com o texto técnico antigo. A frase de “resultado útil” (vigiar) **não** passa pelo filtro de jargão (#99). | só Beta | não — depende de #31 | [ ] adotar  [ ] não |
| 153 | Voltar ao Clássico sai de Hoje | `switchEdition('classic')`: se `viewMode==='hoje'` → `'painel'`; fecha o ⚙. | Sem isto, ao desligar a Beta você ficaria numa tela Hoje que o clássico **não** lista na faixa de cima. | Clássico + Beta (o botão do ⚙) | sim com ressalva: casa com #74 | [ ] adotar  [ ] não |
| 154 | Fallbacks da linha da CDA | `betaCdaPrescText`: `sem dado de prazo`; tratada; conflito Ficha×app; `prazo em acompanhamento`; `Fora da fila` + até data; `sem ciência lançada`. `stripPrescritaIfEstimate` troca “Prescrita” por “prazo vencido (estimado)” se o kind não é `vencido`. | Evita traço e a palavra Prescrita quando o app só estimou. Complementa #102–#104. | só Beta | não — depende de #102 | [ ] adotar  [ ] não |
| 155 | Comentário e log do compilador | `scripts/build.mjs`: cabeçalho “edição Beta”; `console.log` “… Nexus.demo.html — NEXUS Beta”. | Só quem roda o comando de gerar o HTML vê. A tela do usuário é #80. | só código | sim | [ ] adotar  [ ] não |
| 156 | Aliases locais do HTML Beta | O build **continua** gravando `Nexus_demo.html`, `Nexus_demo_experimental.html`, `demo_experimental.html` (não vão para o git). | Atalhos antigos no computador ainda abrem a Beta. Não é tela nova. | só código / disco local | sim | [ ] adotar  [ ] não |
| 157 | Top-nav: esconde a partir do 4º item | `useLayoutEffect` / `measure`: `if (used > limit && i >= 3)` marca `is-overflow`. Reserva espaço para ⚙, Mais e o nome da operação. | Hoje / Painel / Prazos tendem a ficar visíveis; Operações em diante pode ir para **Mais ▾**. | só Beta | não — depende de #72 | [ ] adotar  [ ] não |
| 158 | Texto “Parcelamento vigente” / “Aguardando decisão” / “Ainda impossível” na gaveta | Radar: `silenced[].label = 'Parcelamento vigente'`. Gaveta: `mesaDrawerItems` usa `r.why` ou, se faltar, “Aguardando decisão” (`silenceReason`) e “Ainda impossível” (G5). `PRAZOS_GROUP_LABELS[5]` já era “Ainda impossível”. | QA: linhas da gaveta dizem “Parcelamento vigente · até 17/12/2026”. Sem o rótulo, a gaveta mostraria só o número da CDA. | só Beta | não — depende de #33/#109 | [ ] adotar  [ ] não |
| 159 | Export novo `buildPrescCollectIndex` | Era função interna; agora `export function`. Testes e o app usam via bundle. | Só organização de código. Sem tela própria. | só código | não — depende de #15 | [ ] adotar  [ ] não |
| 160 | Export novo `isPainelPrescCandidate` | Passou de função interna para `export function` com `opts.policy`. | Os testes v2 importam direto. A tela não mostra o nome. | só código | não — depende de #21 | [ ] adotar  [ ] não |
| 161 | Export `PRESC_SNOOZE_REASONS.aguardando_certidao` | Chave `aguardando_certidao` → rótulo “Aguardando certidão”. | Primeira opção do popover Adiar. | só Beta | não — depende de #35/#112 | [ ] adotar  [ ] não |
| 162 | Export razão `peca_protocolada` | “Peça protocolada”. | Usada no QA ao adiar a CDA `90.6.23.000884-40`. | só Beta | não — depende de #35/#112 | [ ] adotar  [ ] não |
| 163 | Export razão `garantia_em_analise` | “Garantia em análise”. | Opção do Adiar. | só Beta | não — depende de #35/#112 | [ ] adotar  [ ] não |
| 164 | Export razão `nao_priorizar_agora` | “Não priorizar agora”. | Opção do Adiar. | só Beta | não — depende de #35/#112 | [ ] adotar  [ ] não |
| 165 | Export razão `outro` + texto obrigatório | “Outro”; motor fura se `note` vazio; UI pede “Descreva”. | Sem texto, não adia. Toast “Descreva o motivo para adiar”. | só Beta | não — depende de #34/#112 | [ ] adotar  [ ] não |
| 166 | Export `snoozeLimitDays` | G1=14, G2=30, G3=7, G5=90, senão 30. | O calendário do Adiar não deixa passar do teto (ex. G1: 18/09 → 02/10). | só Beta | não — depende de #34 | [ ] adotar  [ ] não |
| 167 | Export `MESA_CAP` | `12`. | Teto do bloco de cima. | só Beta | não — #85 | [ ] adotar  [ ] não |
| 168 | Export `MESA_ONE_CLICK` | Set `criar_evento`, `corrigir_ficha`, `vincular_ef`. | Só essas ações de G3 sobem para PRECISA DE VOCÊ. `conferir_autos` e `lancar_ciencia` **não**. | só Beta | não — #86 | [ ] adotar  [ ] não |
| 169 | Export `mesaCertainty` | Função pura. | Selo calculado/estimado/cadastro. | só Beta | não — #88 | [ ] adotar  [ ] não |
| 170 | Export `isG1Vencido` | Grupo 1 e (`prescKind==='vencido'` ou `prescDays<=0`). | Acende o vermelho de VENCIDA. | só Beta | não — #93/#106/#107 | [ ] adotar  [ ] não |
| 171 | Export `mesaNeedsYou` / `splitMesaRows` | Recorte da fila. | Quem entra no bloco de cima vs resto vs G5. | só Beta | não — #86–#87 | [ ] adotar  [ ] não |
| 172 | Export `formatPrescHorizon` | Dias → texto. | “10d”, “há 5d”, “hoje”, “há 2 anos”. | só Beta | não — #103 | [ ] adotar  [ ] não |
| 173 | Export `betaCdaPrescText` | Frase da linha da inscrição. | Nunca “—”; tratada; conflito ficha×app. | só Beta | não — #102 | [ ] adotar  [ ] não |
| 174 | Export `mesaDrawerItems` / `countSnoozeDueThisWeek` / `snoozeMaxUntil` | Gaveta e teto de data. | Silenciados + aviso da semana + max do date picker. | só Beta | não — #109–#112 | [ ] adotar  [ ] não |
| 175 | Export `betaSafeUiText` / `betaEventFamilyLabel` / `betaEventFamilyDesc` | Jargão e rótulos de família. | Textos da Mesa e do modal de evento na Beta. | só Beta | não — #99/#116 | [ ] adotar  [ ] não |
| 176 | CSS `.modal.modal-sticky-footer` | Rodapé grudado do modal. | Usado só na intimação Beta (#128); a regra CSS está no CSS global. | só Beta (uso) | não — #128 | [ ] adotar  [ ] não |
| 177 | CSS KPIs compactos no tema Claro | `.edition-demo.theme-claro .stats-bar` / `.stat-card` nowrap + ellipsis. | Zoom 90 + Claro: cabeçalho da operação numa faixa só. | só Beta | não — #138 | [ ] adotar  [ ] não |
| 178 | CSS `.cda-actions-sticky` | Barra Copiar/Evento/Editar. | Não some ao rolar as colunas. | só Beta | não — #119 | [ ] adotar  [ ] não |
| 179 | CSS `.entity-card .ec-row` gap 6px (Beta) | `.edition-demo .entity-card .ec-row { gap: 6px; }`. | Espaço um pouco maior entre as linhas do card de inscrição. | só Beta | sim | [ ] adotar  [ ] não |
| 180 | Chave `nexus_settings.prazosDeskMode` | Lida no bootstrap; default `mesa`; só `lista` persiste lista. | Se você escolher Lista na Beta e recarregar, continua Lista. O clássico ignora. | só Beta | sim | [ ] adotar  [ ] não |
| 181 | Chave `demoTheme` deixou de ser lida | Removida do objeto `next` gravado no bootstrap. O valor antigo pode permanecer no JSON do navegador, inerte. | Não há como religar Clara/Ardósia/Grafite pelo ⚙. | só Beta | sim | [ ] adotar  [ ] não |
| 182 | `processViewModel` forçado a `D` | Bootstrap grava `'D'`; `updateSetting` não escreve mais `nexus_demo_proc_view`. | Preferência A/B/C antiga é descartada na próxima carga. | só Beta | sim com ressalva: #70 | [ ] adotar  [ ] não |
| 183 | Removido: pílula “Demo” e subtítulos do topbar | Textos do `demo-topbar-sub` (Fila do dia, Radar de prazos, Workspace da operação, “NEXUS Demo Experimental”). | Some toda a faixa explicativa acima do conteúdo. | só Beta | não — #65 | [ ] adotar  [ ] não |
| 184 | Removido: item de trilho “Biblioteca” | Era o atalho para Modelos no rail. | Na Beta o item se chama **Modelos**, na top-nav, como o clássico. | só Beta | não — #64/#71 | [ ] adotar  [ ] não |
| 185 | Removido: item “Carteira” / “Visão geral” | Rail + árvore. | Operações voltam a ser o nome clássico. | só Beta | não — #68 | [ ] adotar  [ ] não |
| 186 | Removido: botão “Resetar / carregar dados demo” na vista Hoje | Estava nos CTAs da Hoje (`!isGAS`). | Só resta no ⚙ (e saiu da sidebar, #50). | só Beta | sim | [ ] adotar  [ ] não |
| 187 | Removido: seletor de fonte “Padrão (Demo)” | Rótulo do botão `font===''` na Demo. | Passa a dizer Public Sans (#59). | só Beta | sim | [ ] adotar  [ ] não |
| 188 | Removido: swatches Clara / Ardósia / Grafite / Mar da Demo | Botões `demo-theme-opt` + `demo-swatch-*`. | Não há mais bolinhas de paleta próprias da Demo. | só Beta | não — #63 | [ ] adotar  [ ] não |
| 189 | Removido: “Central de Comando” no trilho | `demo-rail-brand` “NEXUS / Central de Comando”. | A marca da Beta na Hoje é o kicker “Nova versão (beta)”. | só Beta | não — #64/#75 | [ ] adotar  [ ] não |
| 190 | `attachPrescriptionSnapshots` usa o índice, radar v1 | 6º arg de `classifyPainelPrescAlert` = `{ collectIndex }` **sem** `policy`. | O snapshot gravado na CDA (kind/grupo para outros painéis) no clássico continua v1, só mais rápido. | Clássico + Beta | não — #15 | [ ] adotar  [ ] não |
| 191 | `pendingPetitions` no resultado selado | `sealIntercorrente` copia o array para o objeto de saída. | Permite a frase do pedido pendente (#9) citar a data. | Clássico + Beta | não — #9 | [ ] adotar  [ ] não |
| 192 | Função `requestCloseRespond` criada e **não usada** | Declaração em `App`; o modal de atuação ainda chama `setRespondModal(null)` direto. | Código morto. Esc no modal de ciência **já** fecha via `Modal` (#38), sem a pergunta de texto digitado. | só código | sim (pode apagar depois) | [ ] adotar  [ ] não |
| 193 | `renderCarteiraRankingPanel` permanece no arquivo | Função completa + `{false && …}`. | Código da Demo antiga ainda no JSX, invisível. Não remove capacidade pedida; só não desenha. | só código | sim | [ ] adotar  [ ] não |
| 194 | CSS `.demo-rail` / `.demo-topbar` ainda no shell | Regras antigas + `display:none !important` na Beta. | Peso no CSS, zero na tela. | só código | sim (limpeza) | [ ] adotar  [ ] não |
| 195 | Comentário de `loadDemoData` / alerta | O `alert('✅ Dados demo resetados…')` **não** está no diff. `if (isDemo) setViewMode('hoje')` já existia. | Recarregar dados demo na Beta já ia para Hoje antes; não é novidade deste lote. | — | — | [ ] adotar  [ ] não |

---

## (H) O que NÃO mudou

Confirmado pelo `git diff 1dc76ea` nos caminhos pedidos: **não há** alteração em `Código.js`, `RESUMO-DIARIO.js`, parsers (`parseEprocXLS`, `parseInscricoesXLS`, `parseBensXLS`, `parseProcessosXLS`, PDF PGFN), sync Drive/Planilha (`cloudPush` / `cloudPull` / auto-sync), `generateDemoData` (as 5 operações fictícias são as mesmas), `defaultData()` (formato base), formulários além da validação/textos listados, calculadora compartilhada na regra v1 do clássico.

| # | Item | Técnico | Em linguagem simples | Onde vale | Reversível isoladamente? | Decisão |
|---|---|---|---|---|---|---|
| 196 | Backend Apps Script intocado | Nenhum diff em `Código.js` (`doGet`, backups, Drive). | Publicar só o HTML novo **não** muda a Planilha nem o `doGet` clássico. | — | — | [ ] adotar  [ ] não |
| 197 | E-mail / resumo diário intocado | Nenhum diff em `RESUMO-DIARIO.js`. O plano registrou (fora de escopo): não tratar `interruptAt` como termo; incluir G1 vencidos; janela por grupo. | O e-mail da manhã **continua** com a lógica antiga, mesmo se você adotar as contas novas na tela. | — | — | [ ] adotar  [ ] não |
| 198 | Parsers e importação XLS/PDF | Funções `parse*` e o fluxo da aba Importar **fora** do diff. | “Importar eproc” e XLS da Procuradoria seguem iguais (e **não** foram removidos). | — | — | [ ] adotar  [ ] não |
| 199 | Sync Drive / Planilha | Botões ⬆⬇ e auto-sync no ⚙ inalterados (só o botão demo saiu da sidebar, #50). | Gravar/carregar na Planilha é o mesmo. | — | — | [ ] adotar  [ ] não |
| 200 | Dataset demo (5 operações) | `generateDemoData` sem hunk no diff. | Fachada Norte, Laranjas do Vale, Sucessão Empresarial Sul, Holding Atlântico, Agro Horizonte — mesmos processos, CDAs e audiência 16:00. | — | — | [ ] adotar  [ ] não |
| 201 | Mesa de trabalho de intimações (pin) | Vista `viewMode==='mesa'` (cards pinados) **não** foi o alvo; continua no menu. A “Mesa” nova é a de **Prazos**. | Há duas “Mesas”: a antiga (pins) e a nova (prazos). A antiga não foi apagada. | — | — | [ ] adotar  [ ] não |
| 202 | Campos novos são opcionais | `debt.prescSnooze` só a Beta grava; `debt.statusSource` só a Beta grava. JSON antigo abre nas duas edições. | Rejeitar a Beta **não** exige migrar arquivo. Se alguém já adiar na Beta e voltar ao clássico, o campo fica no JSON parado (o clássico não lê). | — | — | [ ] adotar  [ ] não |

---

## (I) Correção DEBCAD (18/09/2026, ambas as edições)

Correção do leitor PDF `RelatorioCompleto-debcad*.pdf`. Vale no clássico e na Beta: não é feature da Nova versão. Sem commit.

| # | Item | Técnico | Em linguagem simples | Onde vale | Reversível isoladamente? | Decisão |
|---|---|---|---|---|---|---|
| 203 | Seções PROTESTOS e AJUIZAMENTO lidas | `parseDebcadLines` em `src/lib/debcad-parser.js` (chamado por `parseDebcadPDF`). Cabeçalhos sem acento/caixa; para em `FIM DO RELATÓRIO`. `rec.protestos` no mesmo formato do SIDA; `rec.ajuizamentos` vazio se “Não há Ajuizamento.” | O relatório DEBCAD tem blocos de protesto e ajuizamento. Antes o protesto ia parar nas “atualizações” e sumia. Agora o app lê o protesto lavrado (caso AGROTRAC: identificação 202603PR0125624090, efetivação **17/03/2026**). | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 204 | Evento `int_protesto_extrajudicial` criado na importação | O bloco já existente (~import PDF) passa a receber `rec.protestos` também do Debcad. Data = efetivação do evento “Protesto lavrado”. `legalBasis` cita LC 208/2024. | Na CDA 149630450 a ordinária deixa de dizer “consumada em 08/05/2026” e passa ao termo **17/03/2031** (5 anos após o lavrado). Sem o evento, o prazo da inscrição sozinho já teria caído. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 205 | Histórico estruturado `debt.debcad` | Campo opcional na CDA, substituído a cada reimportação (chave = nº da CDA): `{ importedAt, source:'debcad', situation, dadosGerais, history, updates, protestos, ajuizamentos }`. | O histórico deixa de ser um parágrafo ilegível e vira ficha da inscrição. Reimportar o PDF atualiza essa ficha. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 206 | Nota condensada `[Debcad] Histórico` removida | O import não gera mais essa nota. Se já existir na CDA, apaga só ela e registra no log. Demais notas ficam. | A faixa cinza que listava “4 fases: 03/05/2021 — Fase 514…” some. Era o texto que você disse que lia mal. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 207 | Bloco **Histórico DEBCAD** na ficha | `DebcadHistoryBlock` em `renderCdaInlineDetail` e `CdaLegalDetail`. Recolhido por padrão. Aberto: tabela de fases, cards de protesto (marca a linha que gerou o evento de prescrição), linha de ajuizamento, atualizações em sub-bloco recolhido. | Na ficha expandida da CDA aparece um bloco “Histórico DEBCAD” com a contagem (fases · atualizações · protestos · ajuizamento sim/não) e a data da importação. As três colunas de prazo **não** mudam de regra. | Clássico + Beta | sim | [ ] adotar  [ ] não |
| 208 | Módulo + fixture + testes | `src/lib/debcad-parser.js` no `scripts/build.mjs`. Fixture `test/fixtures/debcad/debcad-149630450.lines.json` (linhas pdf.js do PDF real). `test/debcad-parser.test.mjs` (7 testes). | Rede de segurança: se o leitor do protesto quebrar de novo, o teste do AGROTRAC falha. | só código | não — amarra #203 | [ ] adotar  [ ] não |

---

## Dependências

Não dá para recusar um e ficar com o outro:

- **Bloco B (radar v2)** exige **#21** (interruptor) e **#22** (Beta passa `v2`). Sem isso, Mesa, Silenciados, Adiar, `why`/`action` e parcelada-sem-evento **não existem**.
- **Catálogo `why` (#152)** é o texto de **#31**; recusar #152 e adotar a Mesa deixa as linhas sem frase clara.
- **Mesa (#84–#123)** exige **#21–#35** (why, action, silenced, snooze, reviewAt) **e** **#101/#146** (`prazos-mesa.js` no build).
- **Voltar ao Clássico (#153)** casa com **#74** (ida para Hoje).
- **Ações de um clique (#113–#114)** exigem **#31** (`action`) e, no caso parcelada, **#25**.
- **Gaveta Silenciados (#109–#111)** exige **#33** e, para Adiar, **#34**.
- **“PRECISA DE VOCÊ” (#86)** exige `reviewAt` (#32) e `MESA_ONE_CLICK` (#86/101).
- **Linha da CDA na Beta (#102)** exige o radar v2 (#31/#33) e `betaCdaPrescText` (#101).
- **Abrir/Conferir com scroll (#118)** exige `data-cda-id` (#57) e a barra sticky (#119) se quiser os três botões visíveis.
- **Vincular EF (#27 + #113)** exige o flag `incidentOnly` (#16).
- **Esc + clique fora com pergunta (#38–#39)** compartilham `requestCloseModal`.
- **Toasts (#41–#42)** precisam do CSS (#43).
- **Painel ⚙ X / fora / Esc (#44–#47)** compartilham o markup (#48).
- **Layout Beta (#61)** puxa a remoção do trilho/topbar/zonas (#64–#67). Recusar #61 e adotar Hoje/Mesa no trilho antigo **não é possível** neste diff: o trilho foi apagado do JSX.
- **Hoje (#74–#77)** só faz sentido com a top-nav Beta (#71).
- **Agenda unificada (#78)** usa a mesma `renderAgendaWeek` cuja troca Mês→Semana (#55) já vale no Painel clássico.
- **Teste #150** cai se recusar #12.

Itens **relativamente soltos** (dá para recusar sem derrubar o resto): #10 gramática; #18 rótulo art. 40; #50 botão demo na sidebar; #51 títulos ⬇⬆; #52–#53 tooltips; #54 briefing vazio; #56 versão 2.0.7; #75 textos da Hoje; #79 setas ‹ ›; #99 jargão (a Mesa ainda funciona com o texto cru); #127 título Nova intimação; #128 sticky da intimação; #131 selo ATRASADA; #133 toast do relatório; #137 chips zero; #138 KPIs compactos; #139 tooltips abaixo; #140 Aparência; #143 parágrafo 2026.09a.

## Riscos de rejeitar as correções de conta (Bloco A)

Recusar o Bloco A **mantém erros já medidos** na auditoria (dataset em `C:\Users\User\AppData\Local\Temp\nexus-audit\`):

- Recusar **#1** (pausas sobrepostas): termo **1 ano tarde** (embargos+depósito). Alvo do teste: **2025-01-01**.
- Recusar **#2** (segunda ciência): ciclo **reinicia** e o termo pula para a segunda certidão (teste: **2024-01-01** na primeira).
- Recusar **#3–#6** (data futura): adesão/rescisão/pausa **ainda não ocorridas** entram na conta e distorcem fila e termo.
- Recusar **#9**: a tela diz “Prazo em curso” com dias **negativos** quando há petição sem resultado.
- Recusar **#11**: aniversário de 29/02 vira **01/03**.
- Recusar **#12**: ordinária ajuizada continua com status **seguro**.
- Recusar **#13**: coluna diz “sem ciência lançada” mesmo com suspensão do art. 40 datada.

Recusar **só a Beta (Bloco B–F)** e **adotar o Bloco A** é coerente com o plano: o clássico ganha as contas certas e **não** ganha Mesa, Silenciados nem a fila v2. Recusar o Bloco A e adotar a Mesa **não** é coerente: a Mesa lê o radar; as contas erradas apareceriam nas frases.

O e-mail diário (**#197**) **não** acompanha o Bloco A até alguém pedir.

---

*Fim do inventário. Nenhuma alteração foi gravada no git.*
